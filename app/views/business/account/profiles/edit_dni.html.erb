<% content_for :title, "Edita tus datos personales" %>

<div class="account-business-profile-edit">
  <div class="container-edit-profile">
    <div class="products-navigation">
      <ol>
        <li>
          <%= link_to business_account_profile_path do %>
            <span>Mi perfil</span>
            <svg class="icon">
              <use xlink:href="<%= asset_path('icons/business.svg#flecha-separacion-siguiente') %>"></use>
            </svg>
          <% end %>
        </li>
        <li>
          <%= link_to informacion_business_account_profile_path do %>
            <span>Información Personal</span>
            <svg class="icon">
              <use xlink:href="<%= asset_path('icons/business.svg#flecha-separacion-siguiente') %>"></use>
            </svg>
          <% end %>
        </li>
        <li>
          <span>Editar mi número de Documento</span>
        </li>

      </ol>
    </div>
    <div class="profile-header">
      <h1 class="profile-title">Necesitamos tu DNI para validar tus datos</h1>
      <p class="profile-description">Solicitamos tu DNI para verificar tu identidad y evitar errores en el proceso de compra y envío.</p>
      <p class="profile-instructions">Ingresá tu número de documento sin puntos ni espacios para evitar errores en el envío y la facturación.</p>
    </div>
    <div class="profile-form">
      <%= form_with model: @user, url: business_account_profile_path, method: :patch, local: true, class: 'edit-name-form' do |form| %>
        <div class="flex-group">
          <div class="form-group form-group-type">
            <%= form.label :document_type, 'Tipo de Documento', class: 'form-label' %>
            <%= form.select :document_type, User.document_types.keys.map { |type| [type.humanize, type] }, { prompt: 'Selecciona el tipo de documento' }, class: 'form-control custom-select' %>
          </div>
          <div class="form-group">
            <%= form.label :document_id, 'Número de Documento', class: 'form-label' %>
            <%= form.text_field :document_id, value: @user.document_id, class: 'form-control', required: true %>
          </div>
        </div>
        <div class="form-actions">
          <%= form.submit 'Guardar', class: 'btn btn-primary' %>
          <%= link_to 'Volver', informacion_business_account_profile_path, class: 'btn btn-secondary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
